<Window
    x:Class="MyTaskManager.Views.UsuarioFormWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyTaskManager.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding TituloVentana}"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Margin="0,0,0,5" Text="Nombre:" />
            <TextBox Margin="0,0,0,10" Text="{Binding Usuario.Nombre, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Margin="0,0,0,5" Text="Email:" />
            <TextBox Margin="0,0,0,10" Text="{Binding Usuario.Email, UpdateSourceTrigger=PropertyChanged}" />

            <Button
                Width="120"
                Height="30"
                Command="{Binding GuardarCommand}"
                Content="{Binding TextoBoton}" />
        </StackPanel>
    </Grid>
</Window>

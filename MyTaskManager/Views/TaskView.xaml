<UserControl
    x:Class="MyTaskManager.Views.TaskView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyTaskManager.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button
                Command="{Binding AddTareaCommand}"
                Content="Agregar Tarea"
                Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>

        <DataGrid
            Grid.Row="1"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ItemsSource="{Binding Tareas}"
            SelectedItem="{Binding TareaSeleccionada}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Id}"
                    Header="ID" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Titulo}"
                    Header="Título" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Descripcion}"
                    Header="Descripción" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding FechaCreacion}"
                    Header="Fecha Creación" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Usuario.Nombre}"
                    Header="Usuario" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Categoria.Nombre}"
                    Header="Categoria" />
                <DataGridTemplateColumn Width="*" Header="Acciones">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button
                                    Command="{Binding DataContext.DeleteTareaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Content="Borrar"
                                    Style="{StaticResource MaterialDesignOutlinedButton}" />
                                <Button
                                    Command="{Binding DataContext.EditarTareaCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    Content="Editar"
                                    Style="{StaticResource MaterialDesignOutlinedButton}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
